<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="pageLoaded" navigatingTo="navigatingTo">

    <StackLayout>
      <TextField hint="Enter username" text="{{ username }}" />
      <Button text="Join" tap="join"  visibility="{{ isSet }}"/>
</StackLayout>
</Page>
